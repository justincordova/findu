# Navigation
npx expo install @react-navigation/native @react-navigation/stack @react-navigation/bottom-tabs @react-navigation/native-stack
npx expo install react-native-screens react-native-safe-area-context

# State Management
npm install zustand

# Authentication & Backend
npm install @supabase/supabase-js
npm install @react-native-async-storage/async-storage

# Payments
npm install @stripe/stripe-react-native

# UI Components & Styling
npm install react-native-vector-icons
npx expo install expo-linear-gradient
npx expo install react-native-gesture-handler
npx expo install react-native-reanimated

# Location Services
npx expo install expo-location

# Camera & Image Handling
npx expo install expo-image-picker
npx expo install expo-camera
npx expo install expo-image-manipulator

# Notifications
npx expo install expo-notifications
npx expo install expo-device

# Social Auth
npx expo install expo-auth-session expo-crypto
npx expo install expo-web-browser

# Utilities
npx expo install expo-constants
npx expo install expo-secure-store
npx expo install expo-linking
npx expo install expo-haptics
npx expo install expo-font

# Animations
npm install react-native-animatable
npm install lottie-react-native

# Date/Time
npm install date-fns

# Form Handling
npm install react-hook-form
npm install yup @hookform/resolvers

# Icons
npx expo install @expo/vector-icons

# Swipe Cards (for dating app)
npm install react-native-deck-swiper
# or
npm install react-native-tinder-card

# Real-time Chat
npm install socket.io-client

# Image Caching
npx expo install expo-image

# Analytics
npx expo install expo-analytics-amplitude
# or
npm install @react-native-firebase/analytics

# Push Notifications
npm install @react-native-firebase/messaging

# Maps (if needed)
npm install react-native-maps
npx expo install expo-map-view

# Permissions
npx expo install expo-permissions

# In-App Purchases (for premium features)
npm install react-native-purchases

# Video calling (if needed)
npm install @stream-io/video-react-native-sdk